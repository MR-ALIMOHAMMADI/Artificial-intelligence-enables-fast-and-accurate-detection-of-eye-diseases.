
در DBSCAN، پارامتر **ε (اپسیلون)** شعاع همسایگی است و انتخاب درست آن **کل عملکرد الگوریتم** را تعیین می‌کند.
در ادامه **۲ روش کارآمد و استاندارد** برای تأیید مقدار ε معرفی می‌شود.

---

## ✅ روش اول: نمودار k-distance (k-NN Distance Plot)

### ایده‌ی اصلی:

برای هر نقطه، فاصله تا **kاُمین همسایه‌ی نزدیک** محاسبه می‌شود
(معمولاً (k = \text{MinPts})).

### مراحل:

1. MinPts را انتخاب کن (مثلاً 4)
2. برای هر نقطه، فاصله تا 4اُمین همسایه را حساب کن
3. این فاصله‌ها را **به صورت صعودی مرتب کن**
4. نمودار را رسم کن

### معیار انتخاب ε:

* نقطه‌ای که نمودار **شکست (Elbow / Knee)** دارد
* بعد از آن، شیب ناگهان زیاد می‌شود → نویز شروع می‌شود

### توجیه ریاضی:

* نقاط داخل خوشه → فاصله‌ی k-NN کم
* نقاط نویزی → فاصله‌ی k-NN زیاد
* ε باید مرز بین این دو ناحیه باشد

---

### 🔹 مثال عملی:

فرض کن بعد از رسم نمودار k-distance، شکست نمودار در:
[
\varepsilon \approx 0.6
]
دیده می‌شود.

→ ε = 0.6 انتخاب مناسبی است.

---

## ✅ روش دوم: بررسی پایداری خوشه‌ها (Stability Analysis)

### ایده‌ی اصلی:

ε خوب باید:

* تعداد خوشه‌ها را **پایدار**
* و میزان نویز را **معقول** نگه دارد

### مراحل:

1. DBSCAN را برای چند ε نزدیک به هم اجرا کن:
   [
   \varepsilon \in {0.4,;0.5,;0.6,;0.7,;0.8}
   ]
2. برای هر ε:

   * تعداد خوشه‌ها
   * تعداد نقاط نویز
     را بررسی کن
3. εای را انتخاب کن که:

   * تغییرات خوشه‌ها کم باشد
   * نویز نه خیلی زیاد، نه خیلی کم باشد

---

### 🔹 مثال عملی:

فرض کن نتایج این باشد:

| ε   | تعداد خوشه | تعداد نویز |
| --- | ---------- | ---------- |
| 0.4 | 6          | 40         |
| 0.5 | 4          | 20         |
| 0.6 | 4          | 8          |
| 0.7 | 2          | 2          |
| 0.8 | 1          | 0          |

تحلیل:

* ε=0.4 → نویز زیاد
* ε=0.8 → همه‌چیز یک خوشه
* **ε=0.6 → خوشه‌ها پایدار + نویز منطقی**

✅ انتخاب نهایی: ε = 0.6
