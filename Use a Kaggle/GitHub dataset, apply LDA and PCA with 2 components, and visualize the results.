
🔗 Wisconsin Breast Cancer Dataset (Breast Cancer Wisconsin)
لینک صفحه داده در Kaggle:
https://www.kaggle.com/uciml/breast-cancer-wisconsin-data

📌 این دیتاست یک دیتاست طبقه‌بندی سرطان پستان هست و توش چند ویژگی عددی برای هر نمونه داره.
می‌تونی PCA و LDA رو روی داده‌ها اجرا کنی و نتیجه‌ش رو در دو بعد (۲ component) بصری‌سازی (visualize) کنی.


---

روش انجام پروژه (کوتاه)

1. دیتاست رو از Kaggle دانلود کن (فرمت CSV).


2. با پایتون / Scikit-learn:

داده‌ها رو استاندارد کن

PCA با n_components=2

LDA با n_components=2



3. دو نمودار scatter بکش که هر نقطه رنگش بر اساس کلاس (بدخیم / خوش‌خیم) باشه.


4. خروجی‌های دو الگوریتم رو مقایسه کن.
